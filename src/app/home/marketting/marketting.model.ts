export interface DeviceModel {
    code: string;
    value: boolean;
}

export interface WheelModel {
    id: string;
    name: string;
    textColor: string;
    bgColor: string;
    image?: string;
    status: boolean;
}